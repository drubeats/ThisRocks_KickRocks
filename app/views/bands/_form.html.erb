<%= form_with(model: band, local: true) do |form| %>
  <% if band.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(band.errors.count, "error") %> prohibited this band from being saved:</h2>

      <ul>
      <% band.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :band_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :band_description %>
  </div>

  <div class="field">
    <%= form.label :pictureLeft, "Picture Left" %>
    <%= form.file_field :pictureLeft, id: :band_pictureLeft %>
  </div>

  <div class="field">
    <%= form.label :pictureRight, "Picture Right" %>
    <%= form.file_field :pictureRight, id: :band_pictureRight %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
